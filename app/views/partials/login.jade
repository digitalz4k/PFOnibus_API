div(ng-include='\'partials/header\'')
#auth-container
  .row
    .small-6.small-centered.columns.panel.radius
      form.form(name='form', ng-submit='login(form)', novalidate='novalidate')

        .form-group
          label Email
          input.form-control(type='text', name='email', ng-model='user.email')

        .form-group
          label Senha
          input.form-control(type='password', name='password', ng-model='user.password')

        .form-group.has-error
          p.help-block(ng-show='form.email.$error.required && form.password.$error.required && submitted')
            | Digite seu endereço de email e sua senha.
          p.help-block {{ errors.other }}

        button.button.expand.button-primary(type='submit')  Entrar
        span.clearfix

  .row
    .small-6.small-centered.columns
      h5
        small Não tem conta ainda? 
        a.text-center.new-account(href='/signup') Crie uma conta.